# READ WHEN handling compatibility / variance

- Compatibility behavior depends on type position (input vs output).
- Arrays/generic variance can hide mutation hazards.
- In critical paths, prefer immutable transforms and explicit mapping.
